PORT=4000
MONGODB_URI=mongodb://localhost:27017/orii
ORIGIN=http://localhost:5173

# Auth
JWT_SECRET=replace_me_with_a_long_random_string
JWT_EXPIRES_IN=7d

# SMTP for email verification and password resets
SMTP_HOST=smtp.example.com
SMTP_PORT=587
SMTP_USER=your_user
SMTP_PASS=your_pass
SMTP_FROM="ORII <no-reply@orii.in>"

# App base URL for links in emails (frontend)
APP_URL=http://localhost:5173
